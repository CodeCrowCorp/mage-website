import { expect, test } from '@playwright/test'

test('premium page had expected to load', async ({ page }) => {
	// await page.goto('/premium/602863591a512c0401910899')
	// await expect(page).toHaveURL(/.*premium/)
})
